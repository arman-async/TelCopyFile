services:
    bot-bopy:
        container_name: bot-bopy
        image: bot-bopy:beta-1.2.3
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./.env/:/app/.env:ro